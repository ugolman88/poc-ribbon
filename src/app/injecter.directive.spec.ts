import { InjecterDirective } from './injecter.directive';

describe('InjecterDirective', () => {
  it('should create an instance', () => {
    const directive = new InjecterDirective();
    expect(directive).toBeTruthy();
  });
});
